package com.smartlogi.security.enums;

/**
 * Authentication provider types for user accounts.
 * Supports both local authentication and OAuth2 providers.
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE,
    APPLE,
    FACEBOOK,
    OKTA
}
